apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "enterprisebot.fullname" . }}-config
  labels:
    {{- include "enterprisebot.labels" . | nindent 4 }}
data:
  APP_NAME: {{ .Values.config.appName | quote }}
  VERSION: {{ .Values.config.version | quote }}
